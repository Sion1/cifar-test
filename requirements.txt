# Runtime dependencies for the agentic-research-assistant CIFAR-10 demo.
#
# Pinned to the lowest versions known to work; later versions usually fine.
# For the autonomous loop you ALSO need at least one LLM CLI installed
# (claude / codex / gemini) — those aren't pip packages; see README §1.

# Core: training pipeline
torch>=2.0
torchvision>=0.15
pyyaml>=6.0
numpy>=1.21

# Visualization scripts (scripts/visualize_tsne.py + visualize_cam.py)
scikit-learn>=1.0
matplotlib>=3.5

# Optional but strongly recommended (first_launch_setup.sh asks for it):
#   wandb — per-experiment metric tracking. Without it, train.py falls
#   back to local runs/<exp>/history.json.
wandb>=0.15
